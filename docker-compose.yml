version: '3.8'

services:
#  customer-order:
#    build:
#      context: .
#      dockerfile: customer-order/Dockerfile
#    container_name: customer_order
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://customer-order-db:5432/customer_order_db
#      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
#      MANUFACTURING_BASE_URL: http://manufacturing-order:8082
#    depends_on:
#      - customer-order-db
#      - kafka
#      - manufacturing-order
#    restart: unless-stopped
#
#  manufacturing-order:
#    build:
#      context: .
#      dockerfile: manufacturing-order/Dockerfile
#    container_name: manufacturing_order
#    ports:
#      - "8082:8082"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://manufacturing-order-db:5432/manufacturing_order_db
#      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
#    depends_on:
#      - manufacturing-order-db
#      - kafka
#    restart: unless-stopped
#
#  worker:
#    build:
#      context: .
#      dockerfile: worker/Dockerfile
#    container_name: worker
#    ports:
#      - "8083:8083"
#    environment:
#      MANUFACTURING_BASE_URL: http://manufacturing-order:8082
#    depends_on:
#      - manufacturing-order
#    restart: unless-stopped

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://0.0.0.0:29092,CONTROLLER://0.0.0.0:29093,PLAINTEXT_HOST://0.0.0.0:9092'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_LOG_DIRS: '/tmp/kraft-combined-logs'
      CLUSTER_ID: 'MkU3OEVBNTcwNTJENDM2Qk'
  customer-order-db:
    image: postgres:16-alpine
    container_name: pg_customer
    ports:
      - "5455:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=customer_order_db
    volumes:
      - customer_db_data:/var/lib/postgresql/data
    restart: always

  manufacturing-order-db:
    image: postgres:16-alpine
    container_name: pg_manuf
    ports:
      - "5466:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=manufacturing_order_db
    volumes:
      - manufacturing_db_data:/var/lib/postgresql/data
    restart: always

volumes:
  customer_db_data:
  manufacturing_db_data: